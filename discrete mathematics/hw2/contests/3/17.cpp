#include<bits/stdc++.h>

using namespace std;

typedef vector<int> vi;
typedef vector<vector<int>> vvi;

ifstream in("num2brackets.in");
ofstream out("num2brackets.out");

int main() {
    int n, k;
    cin >> n >> k;
    string s;
    int depth = 0;

    vvi dp
    for (int i = 0; i < 2 * n; i++) {
        if ()
    }
}